import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    position: 'absolute',
    width: '80%',
    bottom: 320,
    marginTop: 20,
    fontSize: 18,
    color: '#000',
  },
});
